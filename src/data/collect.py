# Coleta de dados via PubMed/OpenAlex
import requests
import pandas as pd

def coletar_pubmed(query, max_results=100):
    """Coleta IDs de artigos da PubMed."""
    # Implementação aqui
    return []
